{
  "name": "bloggy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && next build && next export",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && next start -p 80",
    "lint": "SET NODE_OPTIONS=--openssl-legacy-provider && next lint",
    "dev-linux": "export NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build-linux": "export NODE_OPTIONS=--openssl-legacy-provider && next build && next export",
    "start-linux": "export NODE_OPTIONS=--openssl-legacy-provider && next start -p 80",
    "lint-linux": "export NODE_OPTIONS=--openssl-legacy-provider && next lint"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^2.1.2",
    "next": "12.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "7.29.0",
    "eslint-config-next": "11.0.1"
  }
}
